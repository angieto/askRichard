using System;

namespace askRichard
{
    class Program
    {
        static void Main(string[] args)
        {
            // randomly select 10 questions

            // match the answer with the question selected

            // change Richard's mood
                // if the user select numbers out of range -> redirect them to select the correct index
                // else if (questions[1-10]) happiness++
                // else if (questions[11-20]) sadness++ 
                // if (question[21-30]) annoyed++
            
            // game conditions
                // if overall points > 8 
                    // win
                    // terminate the game
                // else if overall < 2
                    // lose
                    // terminate the game
                // else 
                    // continue asking questions
        }
    }

     
}
